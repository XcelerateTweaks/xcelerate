import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Xcelerate Store - The Infinity Forge',
  description: "Welcome to the FINAL FORM of PC performance. This isn't just a store. This is a performance temple, a gamer's vault, a creator's armory.",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
} 